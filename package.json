{
  "name": "chess-laziness",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "NODE_PATH=. node ./main.js",
    "start:e2e": "NODE_PATH=. node ./e2e.js",
    "test": "jest --runInBand",
    "test:cov": "jest --runInBand --coverage",
    "test:config": "jest --showConfig",
    "test:clear": "jest --clearCache"
  },
  "engines": {
    "node": "18.16.1"
  },
  "dependencies": {
    "jest": "^29.7.0"
  }
}
